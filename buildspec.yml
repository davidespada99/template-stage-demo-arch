version: 0.2 
os: linux
phases:
  pre_build: 
    commands: 
      - echo no pre-build actions will be executed
  build: 
    commands: 
      - echo Entered the build phase... 
      - echo "This is some Text" > FileName.txt
  post_build: 
    commands: 
      - echo Entered the post_build phase... 
      - echo Everything done.